# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/

# --- Virtual environment (do not track) ---
venv/
.venv/

# --- OS / editor ---
.DS_Store
.vscode/

# --- Large data & generated artifacts (track code, not data) ---

# --- Per-dataset workspaces: track each dataset's small config, NOT its data/outputs ---
datasets/registry.json
datasets/*/data/derived
datasets/*/outputs/

# --- HD map (external dev-kit data, ~48 MB): each dataset's lane_samples.json lives in
#     <dataset>/map/ (from the dev-kit's src/map/map.zip) and enables EXACT
#     georeferencing. geo_reference.py degrades gracefully without it. Track code, not data. ---

